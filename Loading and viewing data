Clear the Environment ---------------------------------
```{r}
rm(list = ls())  # Clear all objects from the workspace
```
Data Import -------------------------------------------
```{r}
my_data <- read.csv("path_to_file.csv")
```

Data Structure ----------------------------------------
```{r}
str(my_data) # View data structure

head(my_data, 5) # top five rows
tail(my_data, 5) # bottom five rows
```
END
